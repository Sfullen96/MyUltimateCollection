import * as common from '../../components/common';
import * as search from '../../components/views/searchResults';

export const Pages = ( {
    Home: common.Home,
	NotFound: common.NotFound,
    SearchResults: search.SearchResults,
} );
